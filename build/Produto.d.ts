declare class Produtos {
    private nome;
    private marca;
    private estoque;
    private preco;
    private precoTotal;
    constructor(nome: string, marca: string, estoque: number, preco: number, precoTotal: number);
}
